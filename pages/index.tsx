import type { NextPage } from 'next';
import Head from 'next/head';
import Image from 'next/image';
import styles from '../styles/Home.module.css';

const Home: NextPage = () => {
    return (
        <div>
            <h1 className="underline font-bold text-3xl text-center">Hello</h1>
        </div>
    );
};

export default Home;
